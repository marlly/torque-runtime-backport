#*
 * author <a href="mailto:fischer@seitenbau.de">Thomas Fischer</a>
 * version $Id$
 *#
package $pkbean;

#if ($child.Ancestor)
  ## need to disassemble the qualified name of the ancestor class,
  ## because the bean package is different
  #set ( $unqualifiedNameStart = $child.Ancestor.lastIndexOf(".") + 1 )
  #set ( $unqualifiedClassName = $child.Ancestor.substring($unqualifiedNameStart) )
  #set ( $parent = "$unqualifiedClassName${beanSuffix}" )
#else
  #set ( $parent = "${table.JavaName}${beanSuffix}" )
#end
/**
#if ($addTimeStamp)
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [$now]
 *
#end
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 */
public class $child.ClassName${beanSuffix} extends $parent
{
}
